#include<iostream>
using namespace std;
 
int main()
{
    int Num1, Num2, Num3;
 
    cout << "Enter first number: ";
    cin >> Num1 ;
    cout << "Enter second number: ";
    cin >> Num2 ;
    cout << "Enter third number: ";
    cin >> Num3 ;
    cout << "Your given numbers are " << Num1 <<", "<< Num2<<" and "<< Num3<< endl; //to give space between num1 and num 2 ,write like this << Num1 <<" "<< Num2
    if (Num1 >> Num2>>Num3)
        cout << "Largest Number is " << Num1;
    else if (Num2 >> Num3>>Num1)
        cout << "Largest Number is " << Num2;
    else
        cout<<"Largest Number is " << Num3;
 
    return 0;
}
